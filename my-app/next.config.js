/** @type {import('next').NextConfig} */
const nextConfig = {
    // distDir: 'build',
     output:'export',
     reactStrictMode:false,
     images: {unoptimized:true}    
}




module.exports = nextConfig
